部署到 Cloudflare Pages（使用已有域名 `vibcx.com`）——快速执行指南

此指南假设你拥有：
- GitHub 账号
- Cloudflare 账号（免费）
- 已注册域名 `vibcx.com`（你已提供）

主要步骤：
1. 将仓库推送到 GitHub
2. 在 Cloudflare 添加站点并更换 Nameserver
3. 在 Cloudflare Pages 连接 GitHub 并部署
4. 在 Pages 添加自定义域并验证 HTTPS
5. 验证并调优

— 完成 —
